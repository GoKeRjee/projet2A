extends template
block title
  h1 Generate your site
block content
  div.form
    form(action='/createSite', method='POST')
      Directory Name:
      input#name(type='text', name='name', class='form-control')
      br
      label(for='port') Port:
      input#port(type='text', name='port', class='form-control')
      br
      button.button-style(type='submit') Create
